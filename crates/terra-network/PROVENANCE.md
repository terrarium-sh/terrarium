# Network library provenance

`config.rs`, `address.rs` (formerly `protocol.rs`), `dns.rs`, and `policy.rs` were extracted from Terra's
`terra-network` crate on 2026-09-08. The source derives from `cryi/smolvm`,
commit `d2573ac`, and is Apache-2.0
under the upstream copyright notices in
[`LICENSE`](LICENSE).
